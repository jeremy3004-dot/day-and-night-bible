---
phase: 01-startup-and-backend-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - src/services/startup/startupService.ts
  - src/stores/authStore.ts
  - src/stores/authSessionState.ts
  - src/services/auth/authService.ts
  - src/services/auth/googleSignIn.ts
  - src/stores/authSessionState.test.ts
  - src/services/supabase/client.ts
  - src/services/startup/startupService.test.ts
  - src/services/auth/googleSignIn.test.ts
  - src/services/supabase/client.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - Cold start reaches onboarding, privacy lock, or the main shell only after critical auth/privacy state is ready.
    - Email sign-up plus Apple and Google auth initialization behave predictably and do not depend on unsafe import-time side effects.
    - A previously authenticated user session restores cleanly after app relaunch.
  artifacts:
    - App.tsx startup gate is explicit and testable.
    - Startup and auth modules have focused regression coverage for critical initialization paths.
    - Supabase client/session storage remains lazy and secure.
  key_links:
    - App startup waits on startupService/authStore/ privacyStore in a stable order.
    - Auth providers feed Supabase session state without bypassing app-level session restoration.
    - Startup tests cover the same contract that the runtime shell depends on.
---

<objective>
Harden the critical startup and authentication contract so the app can cold-start safely, initialize auth predictably, and restore user sessions without gate flicker or provider-specific regressions.

Purpose: This is the first reliability layer for the entire product. If launch and sign-in are unstable, every later feature feels broken.
Output: Updated startup/auth modules plus focused tests that lock in the boot and session-restoration contract.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-startup-and-backend-hardening/01-RESEARCH.md
@.planning/phases/01-startup-and-backend-hardening/01-VALIDATION.md

@App.tsx
@src/services/startup/startupService.ts
@src/stores/authStore.ts
@src/services/auth/authService.ts
@src/services/auth/googleSignIn.ts
@src/services/supabase/client.ts
@src/services/startup/startupService.test.ts
@src/services/auth/googleSignIn.test.ts
@src/services/supabase/client.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock down the startup gate and session restoration order</name>
  <files>App.tsx, src/services/startup/startupService.ts, src/stores/authStore.ts, src/services/supabase/client.ts</files>
  <action>Audit the exact cold-start sequence and make it deterministic: splash handling, critical startup, auth initialization, privacy initialization, onboarding gate selection, and deferred warmups. Keep the existing startup-coordinator pattern, preserve lazy Supabase client creation, and remove any sequencing ambiguity that could allow splash to hide early, sync to run before auth is initialized, or the app to render the wrong first screen.</action>
  <verify>node --test --import tsx src/services/startup/startupService.test.ts src/services/supabase/client.test.ts</verify>
  <done>Critical startup is explicitly ordered, lazy client behavior remains intact, and the startup tests pass against the updated contract.</done>
</task>

<task type="auto">
  <name>Task 2: Harden account creation and provider initialization</name>
  <files>src/services/auth/authService.ts, src/services/auth/googleSignIn.ts, src/stores/authStore.ts, src/stores/authSessionState.ts, src/stores/authSessionState.test.ts, src/services/supabase/client.ts</files>
  <action>Review email/password sign-up plus Apple and Google sign-in behavior and make the auth surface robust for real device builds. Keep provider setup guarded and idempotent, ensure missing or partial env configuration fails safely with explicit app behavior, confirm email sign-up respects the configured Supabase path, and make sure session restoration through authStore remains the single source of truth instead of duplicated provider state.</action>
  <verify>node --test --import tsx src/stores/authSessionState.test.ts src/services/auth/googleSignIn.test.ts src/services/auth/authErrors.test.ts src/services/supabase/client.test.ts</verify>
  <done>Email sign-up and provider setup are safe under partial configuration, authStore remains authoritative for session state, and auth-focused tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Expand targeted regression coverage for startup and auth critical paths</name>
  <files>src/services/startup/startupService.test.ts, src/stores/authSessionState.test.ts, src/services/auth/googleSignIn.test.ts, src/services/supabase/client.test.ts</files>
  <action>Add or refine tests that protect the exact failure modes Phase 1 is addressing: startup sequencing, lazy client creation, email sign-up safety, provider configuration safety, and session-restoration assumptions. Prefer small focused tests near the service modules over broad rewrites, and only add production code changes that directly support testable behavior.</action>
  <verify>node --test --import tsx src/services/startup/startupService.test.ts src/stores/authSessionState.test.ts src/services/auth/googleSignIn.test.ts src/services/supabase/client.test.ts</verify>
  <done>Critical startup/auth assumptions are covered by focused automated tests that would fail if sequencing or configuration safety regressed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/services/startup/startupService.test.ts src/stores/authSessionState.test.ts src/services/auth/googleSignIn.test.ts src/services/supabase/client.test.ts
- [ ] npx eslint App.tsx src/services/startup/startupService.ts src/stores/authStore.ts src/services/auth/authService.ts src/services/auth/googleSignIn.ts src/services/supabase/client.ts
- [ ] Cold-start and session-restoration logic is still aligned with the Phase 1 research contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Startup and auth initialization are more deterministic and more observable than before
- AUTH-01, AUTH-02, and AUTH-03 are materially advanced by code plus regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-startup-and-backend-hardening/01-01-SUMMARY.md`
</output>
