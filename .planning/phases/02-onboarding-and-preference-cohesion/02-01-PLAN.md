---
phase: 02-onboarding-and-preference-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding/localeSetupModel.ts
  - src/screens/onboarding/localeSetupModel.test.ts
  - src/services/onboarding/localeSelection.ts
  - src/services/onboarding/localeSelection.test.ts
  - src/services/onboarding/interfaceLanguageSelection.ts
  - src/services/onboarding/interfaceLanguageSelection.test.ts
  - App.tsx
  - src/screens/onboarding/LocaleSetupFlow.tsx
autonomous: true
requirements:
  - LOCL-01
  - LOCL-02
user_setup: []
must_haves:
  truths:
    - First-run locale setup keeps a coherent step order from interface language through content language selection.
    - Locale search and later settings updates do not create inconsistent persisted country/language state.
    - Existing locale/onboarding behavior is protected with regression coverage before Phase 2 touches adjacent settings flows.
  artifacts:
    - Focused locale and interface-language tests exist near the current search/model code.
    - Any first-run state-transition logic touched by the phase remains explicit rather than implicit.
    - Phase 2 evidence clearly distinguishes already-working locale paths from new settings/privacy work.
  key_links:
    - Locale setup model and locale search services remain the shared contract for onboarding and settings.
    - App-level onboarding handoff still works after locale setup finishes.
    - Later settings locale changes continue to use the same underlying locale data model.
---

<objective>
Protect and lightly tighten the existing locale/onboarding loop so Phase 2 can build on a verified first-run contract instead of assumptions.

Purpose: Locale setup is already the strongest part of this phase. The job here is to preserve that strength with targeted tests and only the smallest code changes needed if a real inconsistency appears.
Output: Locale-focused regression coverage and any minimal fixes required to keep onboarding and later locale changes internally consistent.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-preference-cohesion/02-RESEARCH.md
@.planning/phases/02-onboarding-and-preference-cohesion/02-VALIDATION.md

@App.tsx
@src/screens/onboarding/LocaleSetupFlow.tsx
@src/screens/onboarding/localeSetupModel.ts
@src/screens/onboarding/localeSetupModel.test.ts
@src/services/onboarding/localeSelection.ts
@src/services/onboarding/localeSelection.test.ts
@src/services/onboarding/interfaceLanguageSelection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing locale regression coverage before changing adjacent settings behavior</name>
  <files>src/screens/onboarding/localeSetupModel.test.ts, src/services/onboarding/localeSelection.test.ts, src/services/onboarding/interfaceLanguageSelection.test.ts</files>
  <action>Add or refine small tests that prove the shared locale flow contract: step ordering, localized country search/display behavior, and interface-language search behavior. Prefer pure tests near the current modules instead of broad UI tests.</action>
  <verify>node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/services/onboarding/localeSelection.test.ts src/services/onboarding/interfaceLanguageSelection.test.ts</verify>
  <done>Locale step sequencing and search behavior are covered by focused tests that would catch accidental regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten any real locale-state inconsistency exposed by the tests</name>
  <files>App.tsx, src/screens/onboarding/LocaleSetupFlow.tsx, src/screens/onboarding/localeSetupModel.ts, src/services/onboarding/localeSelection.ts</files>
  <action>Only if a failing test reveals a concrete mismatch, make the smallest production fix necessary to keep first-run and settings locale paths coherent. Avoid redesigning the flow or adding new settings surfaces in this plan.</action>
  <verify>node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/services/onboarding/localeSelection.test.ts src/services/onboarding/interfaceLanguageSelection.test.ts</verify>
  <done>Any real locale-state mismatch exposed during TDD is fixed without expanding scope beyond onboarding and locale preferences.</done>
</task>

<task type="auto">
  <name>Task 3: Reconfirm the locale path stays stable inside the wider app shell</name>
  <files>App.tsx, src/screens/onboarding/LocaleSetupFlow.tsx</files>
  <action>Review the first-run handoff between locale setup and the app shell/auth entrypoint so later settings/privacy work does not accidentally weaken it. Keep changes minimal and evidence-driven.</action>
  <verify>npm test</verify>
  <done>Locale flow changes, if any, remain compatible with the current app-shell onboarding gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/services/onboarding/localeSelection.test.ts src/services/onboarding/interfaceLanguageSelection.test.ts
- [ ] npx eslint App.tsx src/screens/onboarding/LocaleSetupFlow.tsx src/screens/onboarding/localeSetupModel.ts src/services/onboarding/localeSelection.ts src/services/onboarding/interfaceLanguageSelection.ts
- [ ] Locale coverage still maps cleanly to LOCL-01 and LOCL-02 without expanding Phase 2 scope
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The already-implemented locale experience is protected by focused regression evidence
- LOCL-01 and LOCL-02 are materially advanced by code and tests, not assumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-preference-cohesion/02-01-SUMMARY.md`
</output>
