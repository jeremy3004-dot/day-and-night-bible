---
phase: 02-onboarding-and-preference-cohesion
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/screens/more/SettingsScreen.tsx
  - src/screens/more/PrivacyPreferencesScreen.tsx
  - src/screens/more/index.ts
  - src/navigation/MoreStack.tsx
  - src/navigation/types.ts
  - src/services/preferences/reminderPreferences.ts
  - src/services/preferences/reminderPreferences.test.ts
  - src/services/privacy/privacyPreferences.ts
  - src/services/privacy/privacyPreferences.test.ts
  - src/stores/privacyStore.ts
autonomous: true
requirements:
  - PRIV-01
  - PREF-01
user_setup: []
must_haves:
  truths:
    - Returning users can manage privacy and reminder behavior from settings without creating inconsistent local state.
    - Re-enabling reminders with an existing stored time keeps scheduled notifications aligned with the saved preference.
    - Discreet mode remains device-local, understandable, and still relocks after backgrounding.
  artifacts:
    - Settings has a clear privacy entrypoint.
    - Reminder and privacy decision logic is covered by pure tests.
    - Navigation and settings UI remain simple and production-friendly.
  key_links:
    - Settings routes into privacy management through the existing More stack.
    - Reminder UI derives from the same stored `reminderTime` value that sync persists.
    - Privacy settings continue to flow through `privacyStore` and SecureStore-backed services.
---

<objective>
Finish the settings loop for reminders and discreet mode so users can reliably manage those preferences after onboarding.

Purpose: This is the biggest remaining Phase 2 product gap. The app already supports privacy mode and reminders, but the settings surface does not yet make them internally consistent.
Output: Updated settings flow, a dedicated privacy preferences screen, and regression tests for reminder/privacy decision logic.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-preference-cohesion/02-RESEARCH.md
@.planning/phases/02-onboarding-and-preference-cohesion/02-VALIDATION.md

@src/screens/more/SettingsScreen.tsx
@src/stores/authStore.ts
@src/stores/privacyStore.ts
@src/services/privacy/privacyService.ts
@src/services/privacy/privacyMode.ts
@src/hooks/usePrivacyLock.ts
@src/navigation/MoreStack.tsx
@src/navigation/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD the reminder scheduling decisions</name>
  <files>src/services/preferences/reminderPreferences.test.ts, src/services/preferences/reminderPreferences.ts, src/screens/more/SettingsScreen.tsx</files>
  <action>Write failing tests for parsing stored reminder times, seeding the picker from persisted state, and deciding whether enabling reminders should immediately reschedule or open the picker. Then implement the smallest helper needed and update `SettingsScreen` to use it.</action>
  <verify>node --test --import tsx src/services/preferences/reminderPreferences.test.ts</verify>
  <done>Reminder time parsing and reminder-toggle behavior are deterministic and covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: TDD the privacy-settings submission rules</name>
  <files>src/services/privacy/privacyPreferences.test.ts, src/services/privacy/privacyPreferences.ts, src/stores/privacyStore.ts</files>
  <action>Write failing tests for the privacy settings form rules: switching to standard mode, enabling discreet mode with a matching valid PIN, rejecting mismatched PIN confirmation, and allowing a no-op save when discreet mode is already active and the user is not changing the PIN. Then implement the minimal helper/store support needed.</action>
  <verify>node --test --import tsx src/services/privacy/privacyPreferences.test.ts src/services/privacy/privacyMode.test.ts</verify>
  <done>Privacy-settings decisions are explicit, testable, and aligned with the existing local-only privacy model.</done>
</task>

<task type="auto">
  <name>Task 3: Add a clear privacy-management entrypoint in settings</name>
  <files>src/screens/more/SettingsScreen.tsx, src/screens/more/PrivacyPreferencesScreen.tsx, src/screens/more/index.ts, src/navigation/MoreStack.tsx, src/navigation/types.ts</files>
  <action>Add a dedicated settings route/screen for privacy management and wire it into the existing More stack. Reuse existing privacy copy where possible, keep the UI simple, and avoid syncing device-local privacy state to Supabase.</action>
  <verify>node --test --import tsx src/services/preferences/reminderPreferences.test.ts src/services/privacy/privacyPreferences.test.ts src/services/privacy/privacyMode.test.ts</verify>
  <done>Users can manage discreet mode from settings, and the screen uses the same local privacy state that the relock hook depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/services/preferences/reminderPreferences.test.ts src/services/privacy/privacyPreferences.test.ts src/services/privacy/privacyMode.test.ts
- [ ] npx eslint src/screens/more/SettingsScreen.tsx src/screens/more/PrivacyPreferencesScreen.tsx src/screens/more/index.ts src/navigation/MoreStack.tsx src/navigation/types.ts src/services/preferences/reminderPreferences.ts src/services/privacy/privacyPreferences.ts src/stores/privacyStore.ts
- [ ] Reminder state and privacy state are both still aligned with the Phase 2 research contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Returning users can manage privacy and reminder behavior from settings without inconsistent state
- PRIV-01 and PREF-01 are materially advanced by code plus regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-preference-cohesion/02-02-SUMMARY.md`
</output>
