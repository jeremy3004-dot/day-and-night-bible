---
phase: 03-core-reading-and-audio-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/bible/BibleBrowserScreen.tsx
  - src/screens/bible/BibleReaderScreen.tsx
  - src/screens/bible/bibleSearchModel.ts
  - src/screens/bible/bibleSearchModel.test.ts
  - src/services/bible/bibleService.ts
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
user_setup: []
must_haves:
  truths:
    - Users can reach local scripture search from the live Bible surface without network dependency.
    - Search results open into the existing reader flow rather than introducing a parallel reading path.
    - Existing reading-position persistence and daily scripture behavior are preserved while search is added.
  artifacts:
    - A small pure search model exists with focused tests.
    - The Bible browser exposes search without losing the current browse grid.
    - Reader and home behavior remain compatible with existing daily-scripture tests.
  key_links:
    - Bible browser search uses `searchBible()` rather than a new data source.
    - Search results route into the existing `BibleReader` screen.
    - Current reading position in `bibleStore` remains authoritative.
---

<objective>
Expose the repo's existing offline scripture search capability through the live Bible surface while preserving the current browse/read flow.

Purpose: READ-02 is the clearest unmet reading requirement even though the service layer already supports it. This plan should make search real for users with the smallest viable UI and test surface.
Output: Bible browser search UI plus a tested pure helper for search query/result presentation.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-reading-and-audio-polish/03-RESEARCH.md
@.planning/phases/03-core-reading-and-audio-polish/03-VALIDATION.md

@src/screens/bible/BibleBrowserScreen.tsx
@src/screens/bible/BibleReaderScreen.tsx
@src/services/bible/bibleService.ts
@src/services/bible/bibleDatabase.ts
@src/stores/bibleStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD the Bible-search query and result-presentation model</name>
  <files>src/screens/bible/bibleSearchModel.test.ts, src/screens/bible/bibleSearchModel.ts</files>
  <action>Write failing tests for the smallest pure search model needed by the UI: query gating, result-state handling, and verse-reference formatting. Then implement the minimal helper to satisfy those tests.</action>
  <verify>node --test --import tsx src/screens/bible/bibleSearchModel.test.ts</verify>
  <done>The search UI can rely on a tested helper instead of burying all state decisions inline in the screen.</done>
</task>

<task type="auto">
  <name>Task 2: Add local search to the Bible browser without breaking browsing</name>
  <files>src/screens/bible/BibleBrowserScreen.tsx, src/services/bible/bibleService.ts</files>
  <action>Use the existing local Bible service to expose search directly in the Bible browser. Keep the browse grid as the default surface, switch to results only when the search query is active, and route result taps into the existing reader.</action>
  <verify>node --test --import tsx src/services/bible/browserRows.test.ts src/screens/bible/bibleSearchModel.test.ts</verify>
  <done>The live Bible surface supports offline local search while still preserving normal browse behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Reconfirm the reader and daily-scripture contract after search wiring</name>
  <files>src/screens/bible/BibleReaderScreen.tsx</files>
  <action>Make only the smallest reader adjustments needed to consume search result navigation cleanly, then rerun the existing reading/daily-scripture coverage to ensure Phase 3 does not regress the reader contract.</action>
  <verify>npm test</verify>
  <done>Search integrates with the existing reader shell without weakening READ-03 or READ-04 behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/services/bible/browserRows.test.ts src/services/bible/dailyScripture.test.ts src/services/bible/presentation.test.ts src/screens/bible/bibleSearchModel.test.ts
- [ ] npx eslint src/screens/bible/BibleBrowserScreen.tsx src/screens/bible/BibleReaderScreen.tsx src/screens/bible/bibleSearchModel.ts src/screens/bible/bibleSearchModel.test.ts src/services/bible/bibleService.ts
- [ ] Search uses the local Bible database path and remains compatible with current reading-position behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- READ-02 is materially advanced by a live local-search UI
- The existing browse/read loop remains dependable after search is added
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-reading-and-audio-polish/03-01-SUMMARY.md`
</output>
