---
phase: 04-discipleship-and-group-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/types.ts
  - src/navigation/TabNavigator.tsx
  - src/navigation/tabManifest.ts
  - src/navigation/tabManifest.test.ts
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - The existing Learn / Harvest surface is reachable from the live root shell.
    - The root tab contract stays explicit and regression-tested.
    - Existing Home, Bible, and More tab behavior remains intact.
  artifacts:
    - A small pure tab manifest exists with focused tests.
    - The bottom tab navigator mounts LearnStack in the active shell.
    - Harvest uses the existing translated tab label instead of hardcoded copy.
  key_links:
    - Root tab shell now includes LearnStack.
    - `tabs.harvest` is used for the live label.
    - Learn screens remain on the existing LearnStack path.
---

<objective>
Expose the already-built discipleship surface through the active app shell with the smallest safe navigation change.

Purpose: DISC-01 is blocked by shell wiring, not by missing screens. This plan should make the existing learn content reachable without trying to redesign the learning system.
Output: A live Harvest tab plus a small tested tab-shell model that protects the mount point from disappearing again.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discipleship-and-group-rollout/04-RESEARCH.md
@.planning/phases/04-discipleship-and-group-rollout/04-VALIDATION.md

@App.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/TabNavigator.tsx
@src/navigation/LearnStack.tsx
@src/navigation/types.ts
@src/screens/learn/CourseListScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD the root tab shell contract</name>
  <files>src/navigation/tabManifest.test.ts, src/navigation/tabManifest.ts</files>
  <action>Create a tiny pure manifest for root-tab order, label keys, and icons, then add failing tests that require the Learn/Harvest tab to exist between Bible and More.</action>
  <verify>node --test --import tsx src/navigation/tabManifest.test.ts</verify>
  <done>The app shell has an explicit, tested contract for the Harvest tab instead of relying on inline tab config alone.</done>
</task>

<task type="auto">
  <name>Task 2: Mount LearnStack in the live tab shell</name>
  <files>src/navigation/types.ts, src/navigation/TabNavigator.tsx</files>
  <action>Wire LearnStack into the root tab navigator using the tested tab manifest and preserve the existing Home/Bible/More behavior.</action>
  <verify>npx eslint src/navigation/types.ts src/navigation/tabManifest.ts src/navigation/tabManifest.test.ts src/navigation/TabNavigator.tsx</verify>
  <done>The live shell exposes the Harvest tab and routes into the existing learn stack.</done>
</task>

<task type="auto">
  <name>Task 3: Reconfirm app-wide navigation stability after the mount</name>
  <files>src/navigation/TabNavigator.tsx</files>
  <action>Run the focused tab-shell tests and the broader repo suite to ensure the new root tab does not disturb unrelated app flows.</action>
  <verify>npm test</verify>
  <done>DISC-01 is materially advanced without destabilizing the app shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/navigation/tabManifest.test.ts
- [ ] npx eslint src/navigation/types.ts src/navigation/tabManifest.ts src/navigation/tabManifest.test.ts src/navigation/TabNavigator.tsx
- [ ] npm test
- [ ] The active root shell now exposes the existing learn/disciple surface through a Harvest tab
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- DISC-01 is materially advanced by live-shell reachability rather than hidden navigation
- Existing Home, Bible, and More tabs continue working after the learn surface is mounted
</success_criteria>

<output>
After completion, create `.planning/phases/04-discipleship-and-group-rollout/04-01-SUMMARY.md`
</output>
