---
phase: 04-discipleship-and-group-rollout
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/services/groups/groupService.ts
  - src/services/groups/groupService.test.ts
  - src/screens/learn/GroupSessionScreen.tsx
autonomous: true
requirements:
  - GROUP-02
user_setup: []
must_haves:
  truths:
    - Synced group-session capture is guarded by real backend/auth availability checks.
    - Session recording failures are surfaced cleanly instead of silently assuming success.
    - The repo carries regression coverage for the most important group-service guardrails.
  artifacts:
    - Group service guard/behavior tests exist.
    - Group session capture path is explicit about when it is local-only vs synced.
    - Backend permission assumptions are documented by tests rather than implied by UI alone.
  key_links:
    - `recordSyncedGroupSession` stays the canonical synced session path.
    - Group session UI does not claim backend persistence when config/auth preconditions are missing.
    - GROUP-02 verification is grounded in service behavior, not only screen presence.
---

<objective>
Verify and harden the synced group-session capture path so backend-protected study activity is either truly supported or clearly unavailable.

Purpose: GROUP-02 is the highest-risk Phase 4 backend promise. This plan should validate the service guards and make the UI/service boundary explicit before the product overclaims secure synced session tracking.
Output: Guarded group-service tests plus the smallest session-surface adjustments needed to keep backend expectations honest.
</objective>

<verification>
- [ ] node --test --import tsx src/services/groups/groupService.test.ts
- [ ] npx eslint src/services/groups/groupService.ts src/services/groups/groupService.test.ts src/screens/learn/GroupSessionScreen.tsx
- [ ] npm test
</verification>

<output>
After completion, create `.planning/phases/04-discipleship-and-group-rollout/04-03-SUMMARY.md`
</output>
