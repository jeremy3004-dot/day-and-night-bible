---
phase: 05-release-hardening-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/release-smoke-checklist.md
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - One repo command reruns the highest-signal regression checks for startup, auth, sync, reading, audio, and group release risks.
    - Manual release smoke checks are written down in one short place instead of living in chat history.
    - The release regression gate stays intentionally focused instead of becoming a second copy of the entire suite.
  artifacts:
    - A new `test:release` script exists in `package.json`.
    - A release smoke checklist documents the manual device/distribution checks that automation still cannot prove.
    - The chosen release regression tests map clearly back to the roadmap's critical user journeys.
  key_links:
    - REL-01 is satisfied by explicit evidence, not by assuming `npm test` is enough.
    - Manual release checks remain separate from automated regression checks.
---

<objective>
Create the smallest repeatable regression gate for the app's critical release paths.

Purpose: before shipping, the repo should have one obvious command that exercises the highest-risk startup, auth, sync, reading, audio, and group logic checks, plus a short manual smoke checklist for what still must be confirmed on device or in distribution systems.
Output: `test:release` plus a concise release smoke checklist.
</objective>

<verification>
- [ ] npm run test:release
- [ ] npm test
</verification>

<output>
After completion, create `.planning/phases/05-release-hardening-and-distribution/05-01-SUMMARY.md`
</output>
