---
phase: 05-release-hardening-and-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - src/services/startup/releaseMetadata.test.ts
  - src/services/startup/runtimeConfig.test.ts
  - src/services/groups/groupRepository.ts
  - src/services/groups/groupService.ts
  - src/services/onboarding/interfaceLanguageSelection.test.ts
  - src/stores/authSessionState.test.ts
  - ios/EveryBible.xcodeproj/project.pbxproj
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - REL-02
user_setup: []
must_haves:
  truths:
    - TypeScript compile safety is part of release readiness instead of an ignored side channel.
    - Checked-in Expo and native release metadata are aligned for the current versioning strategy.
    - Release docs describe the actual internal-build and production/TestFlight paths in the repo today.
  artifacts:
    - A `typecheck` script exists and passes.
    - A focused release metadata contract test guards version/build alignment and EAS release-source expectations.
    - The final release verification command combines lint, typecheck, and the focused release regression gate.
  key_links:
    - REL-02 is enforced through tests and scripts, not only documentation.
    - Existing Phase 5 docs and release scripts stay consistent with `eas.json`.
---

<objective>
Make release readiness honest by fixing compile failures, aligning version metadata, and codifying the real release path in tests and docs.

Purpose: the repo should not claim release readiness while typecheck fails, version metadata drifts, or release docs describe the wrong distribution profile. This plan closes those gaps with the smallest durable contracts possible.
Output: passing typecheck, release metadata contract coverage, aligned iOS marketing version, and a single release verification command.
</objective>

<verification>
- [ ] npm run typecheck
- [ ] node --test --import tsx src/services/startup/runtimeConfig.test.ts src/services/startup/releaseMetadata.test.ts
- [ ] npm run release:verify
</verification>

<output>
After completion, create `.planning/phases/05-release-hardening-and-distribution/05-02-SUMMARY.md`
</output>
