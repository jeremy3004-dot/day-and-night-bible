# Plan 12-01: Define Shared Design Tokens And Typography

## Objective

Create one disciplined mobile design-system layer for typography, spacing, radius, shadow, and refined semantic colors.

## Scope

- Add a shared `src/design/system.ts` module
- Refine the current theme palette toward cleaner neutrals while preserving EveryBible's accent red
- Standardize navigation typography and shell-level sizing

## Verification

```bash
node --test --import tsx src/design/designSystemSource.test.ts
npm run lint -- src/design/system.ts src/contexts/ThemeContext.tsx src/navigation/RootNavigator.tsx src/design/designSystemSource.test.ts
npm run typecheck
```
