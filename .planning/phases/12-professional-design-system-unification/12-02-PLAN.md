# Plan 12-02: Apply Design System To Shell, Home, And Bible Browse

## Objective

Move the app shell and the most-visible Bible and Home surfaces onto the shared system so first impression improves immediately.

## Scope

- Update tab bar and navigation typography/chrome
- Apply shared spacing, radius, and card hierarchy to `HomeScreen`
- Apply shared spacing, radius, and card hierarchy to `BibleBrowserScreen`
- Keep the simplified `ChapterSelectorScreen` aligned to the same system

## Verification

```bash
node --test --import tsx src/design/designSystemSource.test.ts src/screens/bible/chapterSelectorChromeSource.test.ts
npm run lint -- src/navigation/TabNavigator.tsx src/navigation/RootNavigator.tsx src/screens/home/HomeScreen.tsx src/screens/bible/BibleBrowserScreen.tsx src/screens/bible/ChapterSelectorScreen.tsx src/design/designSystemSource.test.ts
npm run typecheck
```
