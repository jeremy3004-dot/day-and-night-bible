---
phase: 14-backend-driven-bible-content-sync-and-offline-pack-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/bible-media-platform-policy.md
  - docs/bible-media-rollback-runbook.md
  - supabase/migrations/<timestamp>_create_translation_audio_versions.sql
  - src/services/supabase/types.ts
  - src/services/translations/translationService.ts
  - package.json
  - app.json
  - src/types/bible.ts
  - src/stores/bibleStore.ts
  - src/stores/persistedStateSanitizers.ts
  - src/services/audio/audioDownloadStorage.ts
  - src/services/audio/audioDownloadService.ts
  - src/services/audio/audioDownloadService.test.ts
  - src/services/bible/bibleDataModel.ts
  - src/services/bible/bibleDatabaseSource.test.ts
autonomous: true
requirements:
  - READ-01
  - READ-02
  - AUDIO-03
  - MULTI-01
user_setup:
  - Confirm the backend team can expose a signed catalog/manifest contract with stable translation IDs, versions, URLs, and checksum/signature metadata.
must_haves:
  truths:
    - The app can ingest a runtime translation catalog without dropping seeded bundled translations.
    - Supabase can publish a current audio version and manifest per translation without the app hardcoding bucket rules.
    - Signed-manifest verification is proven on device/runtime before it becomes a hard dependency for pack activation.
    - Large translation/audio downloads can survive backgrounding and app relaunch.
  artifacts:
    - Failing tests cover runtime catalog sanitization and manifest verification plumbing.
    - A rollback-safe backend schema exists for current audio version, manifest URL, storage provider, and delivery mode.
    - Downloader integration proof exists for start, reattach, and failure reporting.
    - Planning docs capture the exact manifest fields the mobile client expects.
  key_links:
    - Supabase audio version rows -> runtime catalog fields exposed to the client.
    - bibleStore runtime catalog -> persisted state sanitization.
    - downloader job lifecycle -> pack install state machine.
    - manifest verification -> later pack install/activation plans.
---

<objective>
Establish the Supabase delivery contract and runtime foundation for backend-driven Bible packs.

Purpose: make runtime translations, signed manifests, cache-safe audio publication, and resilient background downloads real prerequisites instead of assumptions.
Output: proven Supabase catalog/audio-version shape + downloader integration + signature-verification path that later plans can safely build on.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-CONTEXT.md
@.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-RESEARCH.md
@docs/bible-media-platform-policy.md
@docs/bible-media-rollback-runbook.md

@supabase/migrations/20260322140700_create_content_versioning.sql
@package.json
@app.json
@src/services/supabase/types.ts
@src/services/translations/translationService.ts
@src/types/bible.ts
@src/stores/bibleStore.ts
@src/stores/persistedStateSanitizers.ts
@src/services/audio/audioDownloadStorage.ts
@src/services/audio/audioDownloadService.ts
@src/services/audio/audioDownloadService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Supabase audio-version control plane and manifest contract</name>
  <files>docs/bible-media-platform-policy.md, docs/bible-media-rollback-runbook.md, supabase/migrations/&lt;timestamp&gt;_create_translation_audio_versions.sql, src/services/supabase/types.ts, src/services/translations/translationService.ts, src/types/bible.ts, src/stores/bibleStore.ts, src/stores/persistedStateSanitizers.ts, src/services/bible/bibleDatabaseSource.test.ts</files>
  <action>Add failing tests and the minimal schema/types/store shape needed to preserve seeded bundled translations while accepting backend-provisioned translations, audio version metadata, manifest URLs, active installed version IDs, and failure/install states. Use Supabase CLI migration workflow so the backend contract is explicit and reviewable.</action>
  <verify>supabase migration new create_translation_audio_versions && supabase db push --dry-run && node --test --import tsx src/services/bible/bibleDatabaseSource.test.ts src/stores/persistedStateSanitizers.test.ts</verify>
  <done>Supabase can publish current audio-version metadata per translation, and the app can model runtime translations/install states without treating them as invalid persisted data.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the background download engine and job-lifecycle proof</name>
  <files>package.json, app.json, src/services/audio/audioDownloadStorage.ts, src/services/audio/audioDownloadService.ts, src/services/audio/audioDownloadService.test.ts</files>
  <action>Install and wire `@kesha-antonov/react-native-background-downloader`, add the smallest durable job model needed for start/reattach/failure reporting, and document any native/config-plugin requirements exposed by the integration.</action>
  <verify>node --test --import tsx src/services/audio/audioDownloadService.test.ts && npm run typecheck</verify>
  <done>Large-file download jobs can be represented durably enough that later plans can bind them to text/audio pack installs.</done>
</task>

<task type="auto">
  <name>Task 3: Prove manifest verification and storage publication policy on the actual runtime contract</name>
  <files>package.json, src/types/bible.ts, src/services/bible/bibleDataModel.ts, docs/bible-media-platform-policy.md, docs/bible-media-rollback-runbook.md</files>
  <action>Install `jose`, add a narrow verification proof for the chosen signature shape, and document the exact storage publication rules for versioned immutable paths, cache headers, and rollback. Fail loudly if the runtime cannot verify the manifest on current iOS/Android targets.</action>
  <verify>npm run typecheck && npm test</verify>
  <done>The phase exits with either a working JOSE verification path or an explicitly documented fallback to checksum-only verification, plus a written storage/rollback contract the next plans must obey.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] supabase db push --dry-run
- [ ] node --test --import tsx src/services/audio/audioDownloadService.test.ts src/stores/persistedStateSanitizers.test.ts src/services/bible/bibleDatabaseSource.test.ts
- [ ] npm run typecheck
- [ ] npm run lint
- [ ] Manual proof on at least one iOS device/simulator for downloader background reattach and one manifest verification path
</verification>

<success_criteria>

- All tasks completed
- Verification commands pass
- Runtime translation catalog shape is no longer hardcoded to the compile-time bundled list
- Supabase owns an explicit, rollback-safe audio-version contract instead of implicit bucket conventions
- Background download and signed-manifest verification are proven enough for the install/activation plans
- Any runtime/platform caveat is documented explicitly instead of being discovered mid-implementation
</success_criteria>

<output>
After completion, create `.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-01-SUMMARY.md`
</output>
