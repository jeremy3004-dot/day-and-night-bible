---
phase: 14-backend-driven-bible-content-sync-and-offline-pack-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/services/bible/bibleDatabase.ts
  - src/services/bible/bibleService.ts
  - src/services/bible/bibleDataModel.ts
  - src/stores/bibleStore.ts
  - src/types/bible.ts
  - src/services/bible/bibleDatabaseSource.test.ts
  - src/services/bible/bibleDataModel.test.ts
autonomous: true
requirements:
  - READ-01
  - READ-02
  - MULTI-01
must_haves:
  truths:
    - The active reader/search path resolves through an installed translation-pack registry, not one hardcoded bundled DB.
    - A candidate pack never replaces the current active pack until verify/install succeeds fully.
    - The app can keep bundled seed translations working while installing remote ones.
  artifacts:
    - Failing tests cover install, activation, rollback, and stale-candidate rejection.
    - The pack registry persists enough metadata to recover after restart.
    - Query routing stays translation-correct while different translations live in different pack files/versions.
  key_links:
    - pack registry -> bibleDatabase path resolution.
    - install/rollback state -> bibleService query/readiness calls.
---

<objective>
Build the versioned SQLite text-pack install and activation layer.

Purpose: make backend-provisioned translations readable and searchable offline without breaking the current bundled local-first experience.
Output: pack registry + atomic install/rollback + translation-aware query routing through the existing Bible service surface.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-CONTEXT.md
@.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-RESEARCH.md
@.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-01-PLAN.md

@src/services/bible/bibleDatabase.ts
@src/services/bible/bibleService.ts
@src/stores/bibleStore.ts
@src/types/bible.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD pack registry, activation, and rollback behavior</name>
  <files>src/services/bible/bibleDatabaseSource.test.ts, src/services/bible/bibleDataModel.test.ts, src/stores/bibleStore.ts, src/types/bible.ts</files>
  <action>Add failing tests that capture install candidate staging, activation only after verification, last-known-good preservation, rollback availability, and restart-safe persisted metadata.</action>
  <verify>node --test --import tsx src/services/bible/bibleDatabaseSource.test.ts src/services/bible/bibleDataModel.test.ts</verify>
  <done>The pack lifecycle is fully specified in tests before DB-path changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the Bible database layer to resolve translation -> installed SQLite path</name>
  <files>src/services/bible/bibleDatabase.ts, src/services/bible/bibleService.ts, src/services/bible/bibleDataModel.ts</files>
  <action>Replace the single-bundled-DB assumption with a path resolver that can open the active installed pack for a translation while preserving the seeded fallback path for bundled content.</action>
  <verify>node --test --import tsx src/services/bible/bibleDatabaseSource.test.ts src/services/bible/browserRows.test.ts src/screens/bible/bibleSearchModel.test.ts</verify>
  <done>Chapter reads and search queries work against the active installed translation pack without requiring screen-level changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/services/bible/bibleDatabaseSource.test.ts src/services/bible/bibleDataModel.test.ts src/services/bible/browserRows.test.ts src/screens/bible/bibleSearchModel.test.ts
- [ ] npm run typecheck
- [ ] npm run lint
- [ ] Manual smoke test opens a seeded translation and an installed translation, then verifies search and chapter reads still work offline
</verification>

<success_criteria>

- All tasks completed
- Verification commands pass
- Versioned SQLite packs can be installed, activated, and rolled back safely
- Current reader/search consumers remain stable at their call sites
- The app can support remote translations without assuming a single global bundled DB
</success_criteria>

<output>
After completion, create `.planning/phases/14-backend-driven-bible-content-sync-and-offline-pack-delivery/14-02-SUMMARY.md`
</output>
