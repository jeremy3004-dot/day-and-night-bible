---
phase: 15-reverential-theme-typography
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/screens/home/HomeScreen.tsx
  - src/screens/bible/BibleBrowserScreen.tsx
  - src/screens/bible/BibleReaderScreen.tsx
  - src/screens/bible/ChapterSelectorScreen.tsx
  - src/components/audio/AudioFirstChapterCard.tsx
  - src/components/audio/AudioPlayerBar.tsx
  - src/components/audio/AudioProgressScrubber.tsx
  - src/components/audio/MiniPlayer.tsx
  - src/components/audio/PlaybackControls.tsx
  - src/components/bible/CompanionCard.tsx
  - src/components/bible/CompanionSection.tsx
  - src/components/skeleton/CardSkeleton.tsx
  - src/components/skeleton/Skeleton.tsx
  - src/components/skeleton/VersesSkeleton.tsx
  - src/navigation/TabNavigator.tsx
autonomous: true
requirements:
  - M2-DESIGN-01

must_haves:
  truths:
    - "HomeScreen uses warm palette with serif verse display and no nested card-in-card patterns"
    - "BibleReaderScreen hardcoded rgba values are replaced with theme tokens including glassBackground"
    - "Bible browser and chapter selector use reduced border radius and warm palette"
    - "Audio components (player bar, mini player, playback controls) use warm palette and reduced radius"
    - "Tab bar uses warm palette colors for active/inactive states"
  artifacts:
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "Warm-palette home with serif verse display"
      contains: "colors.background"
    - path: "src/screens/bible/BibleReaderScreen.tsx"
      provides: "Reader with theme-aware glass effects"
      contains: "glassBackground"
    - path: "src/navigation/TabNavigator.tsx"
      provides: "Warm-toned tab bar"
      contains: "colors.tabActive"
  key_links:
    - from: "src/screens/bible/BibleReaderScreen.tsx"
      to: "src/contexts/ThemeContext.tsx"
      via: "glassBackground token replacing hardcoded rgba values"
      pattern: "colors\\.glassBackground"
    - from: "src/screens/home/HomeScreen.tsx"
      to: "src/design/system.ts"
      via: "readingDisplay typography for verse-of-the-day"
      pattern: "typography\\.readingDisplay"
---

<objective>
Apply the reverential visual language to the highest-traffic surfaces: Home, Bible browser/reader/chapter selector, audio components, skeleton loaders, and the tab bar. Strip nested cards, excessive radius, decorative shadows, and hardcoded colors.

Purpose: These are the surfaces users see most. Applying the warm palette and stripped patterns here delivers the majority of the visual transformation.

Output: Updated screens and components using warm palettes, reduced radius, Lora serif on reading surfaces, no hardcoded colors, no nested cards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-reverential-theme-typography/15-CONTEXT.md
@.planning/phases/15-reverential-theme-typography/15-RESEARCH.md
@.planning/phases/15-reverential-theme-typography/15-01-SUMMARY.md

<interfaces>
<!-- Tokens available from Plan 01 -->

From src/contexts/ThemeContext.tsx (after Plan 01):
```typescript
interface ThemeContextValue {
  colors: ThemeColors;  // includes glassBackground
  isDark: boolean;
  isLowLight: boolean;
  themeMode: 'dark' | 'light' | 'low-light';
  setTheme: (mode: 'dark' | 'light' | 'low-light') => void;
  toggleTheme: () => void;
}
```

ThemeColors now includes `glassBackground: string`.

From src/design/system.ts (after Plan 01):
```typescript
export const radius = { xs: 2, sm: 4, md: 8, lg: 12, pill: 999 } as const;
export const shadows = {
  card: {} as ViewStyle,  // Empty — no decorative shadows
  floating: Platform.select<ViewStyle>({ ios: { ... }, android: { elevation: 4 } }),
} as const;
// readingFontFamily = 'Lora-Regular'
// readingFontFamilyItalic = 'Lora-Italic'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep Home, Bible browser, chapter selector, and tab bar</name>
  <files>
    src/screens/home/HomeScreen.tsx,
    src/screens/bible/BibleBrowserScreen.tsx,
    src/screens/bible/ChapterSelectorScreen.tsx,
    src/navigation/TabNavigator.tsx
  </files>
  <read_first>
    src/screens/home/HomeScreen.tsx,
    src/screens/bible/BibleBrowserScreen.tsx,
    src/screens/bible/ChapterSelectorScreen.tsx,
    src/navigation/TabNavigator.tsx,
    src/design/system.ts,
    src/contexts/ThemeContext.tsx
  </read_first>
  <action>
**Per D-13, D-14, D-15, D-16, D-17, D-18 — apply the reverential visual language to each file:**

**General pattern for ALL files in this task (and all subsequent sweep tasks):**
1. Replace any hardcoded hex colors (`#ffffff`, `#fff`, `#000`, `rgba(...)` that are not theme-derived) with the appropriate `colors.*` token from useTheme()
2. Replace any `borderRadius: radius.sm` (old 12) with the new token values — most card surfaces should use `radius.sm` (now 4) or `radius.xs` (now 2). Only pill shapes keep `radius.pill`.
3. Remove any `...shadows.card` spreads (they are now empty but should be cleaned out for clarity)
4. Remove nested card-in-card patterns — flatten by using spacing/dividers between content sections instead of wrapping inner content in additional card Views
5. Do NOT change layout (flex direction, alignment, component ordering), navigation (screen names, params), or feature logic (onPress handlers, data fetching)

**HomeScreen.tsx specifics:**
- The verse-of-the-day display: ensure it uses `typography.readingDisplay` (which now uses Lora Italic). The verse text should render in serif.
- Remove any nested card wrapping around the verse card. If the verse is inside a card that is inside another card, flatten to one surface with `colors.cardBackground` and `borderRadius: radius.sm`.
- Replace any `shadows.card` usage with plain `borderWidth: 1, borderColor: colors.cardBorder` or just spacing.
- Section titles should stay sans-serif (system font via `typography.sectionTitle`).

**BibleBrowserScreen.tsx specifics:**
- Book list items: reduce border radius to `radius.sm` (4).
- Remove any decorative shadows on book items.
- Ensure background uses `colors.background`.

**ChapterSelectorScreen.tsx specifics:**
- Chapter number tiles: reduce border radius to `radius.xs` (2) for a clean grid look.
- Active/selected chapter uses `colors.accentPrimary` background.
- Remove any card shadows on the chapter grid.

**TabNavigator.tsx specifics:**
- Update tab bar styling to use warm palette: `backgroundColor: colors.background` (or a slightly elevated `colors.cardBackground`).
- `tabBarActiveTintColor: colors.tabActive`, `tabBarInactiveTintColor: colors.tabInactive`.
- Remove any frosted glass effect if present (keep solid warm background; frosted glass deferred per research open question about Android BlurView quality).
- Tab bar border: use `borderTopColor: colors.cardBorder, borderTopWidth: StyleSheet.hairlineWidth`.
  </action>
  <verify>
    <automated>cd /Users/dev/conductor/workspaces/EveryBible/columbus && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/home/HomeScreen.tsx does NOT contain `#ffffff` or `#fff` (case-insensitive grep)
    - src/screens/home/HomeScreen.tsx contains `colors.background` or `colors.cardBackground`
    - src/screens/bible/BibleBrowserScreen.tsx does NOT contain hardcoded hex colors outside of theme token usage
    - src/screens/bible/ChapterSelectorScreen.tsx contains `radius.xs` or `radius.sm` (not old `radius.lg` or `radius.xl`)
    - src/navigation/TabNavigator.tsx contains `colors.tabActive` and `colors.tabInactive`
    - No file in this task contains `shadows.card` spread (grep for `...shadows.card`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Home, Bible browser, chapter selector, and tab bar all use warm palette tokens, reduced radius, no nested cards, no decorative shadows. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep BibleReader and all audio/bible/skeleton components</name>
  <files>
    src/screens/bible/BibleReaderScreen.tsx,
    src/components/audio/AudioFirstChapterCard.tsx,
    src/components/audio/AudioPlayerBar.tsx,
    src/components/audio/AudioProgressScrubber.tsx,
    src/components/audio/MiniPlayer.tsx,
    src/components/audio/PlaybackControls.tsx,
    src/components/bible/CompanionCard.tsx,
    src/components/bible/CompanionSection.tsx,
    src/components/skeleton/CardSkeleton.tsx,
    src/components/skeleton/Skeleton.tsx,
    src/components/skeleton/VersesSkeleton.tsx
  </files>
  <read_first>
    src/screens/bible/BibleReaderScreen.tsx,
    src/components/audio/AudioFirstChapterCard.tsx,
    src/components/audio/AudioPlayerBar.tsx,
    src/components/audio/AudioProgressScrubber.tsx,
    src/components/audio/MiniPlayer.tsx,
    src/components/audio/PlaybackControls.tsx,
    src/components/bible/CompanionCard.tsx,
    src/components/bible/CompanionSection.tsx,
    src/components/skeleton/CardSkeleton.tsx,
    src/components/skeleton/Skeleton.tsx,
    src/components/skeleton/VersesSkeleton.tsx,
    src/contexts/ThemeContext.tsx,
    src/design/system.ts
  </read_first>
  <action>
**BibleReaderScreen.tsx — Per D-18, research pitfall #4 (glass effects):**

This file has 5 known hardcoded rgba values for glass/blur effects. Replace each one:
- Any `rgba(255, 255, 255, ...)` glass overlay → `colors.glassBackground` token
- Any `rgba(0, 0, 0, ...)` glass overlay → `colors.overlay` token
- Any hardcoded hex background → appropriate `colors.bibleBackground` / `colors.bibleSurface` / `colors.bibleElevatedSurface`
- Scroll-collapse glass header: use `colors.glassBackground` instead of hardcoded rgba
- Any `isDark ? 'rgba(...)' : 'rgba(...)'` ternary → single `colors.glassBackground` (the token already varies per theme)

Also in BibleReaderScreen:
- Reduce border radius on any card/surface elements to `radius.sm` (4) or `radius.xs` (2)
- Reader content area should use `colors.bibleBackground` for background
- Verse text should already use `typography.readingBody` (which now references Lora) — verify and fix if not
- Section headings in the reader should use `typography.readingHeading` (now Lora uppercase)

**Audio components — general sweep pattern:**

For each audio component:
1. Replace any hardcoded colors (including `rgba(...)` values) with theme tokens
2. Reduce border radius: control buttons → `radius.sm` (4), pill-shaped progress bars → keep `radius.pill`
3. Remove decorative shadows
4. Scrubber track: use `colors.cardBorder` for inactive track, `colors.accentPrimary` for active fill
5. MiniPlayer: use `colors.cardBackground` with `borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: colors.cardBorder` instead of shadow
6. PlaybackControls: ensure play/pause/transport buttons use `colors.primaryText` for icons on `colors.background`

**Bible components (CompanionCard, CompanionSection):**
- Reduce card border radius to `radius.sm` (4)
- Remove shadows.card usage
- Use `colors.cardBackground` with `borderWidth: 1, borderColor: colors.cardBorder`

**Skeleton components:**
- Update skeleton shimmer/placeholder colors to use warm-tinted values: `colors.cardBackground` for base, `colors.cardBorder` for shimmer highlight
- Reduce border radius on skeleton shapes to match new `radius.sm`
  </action>
  <verify>
    <automated>cd /Users/dev/conductor/workspaces/EveryBible/columbus && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/bible/BibleReaderScreen.tsx does NOT contain `rgba(255` (hardcoded white glass)
    - src/screens/bible/BibleReaderScreen.tsx contains `colors.glassBackground`
    - src/screens/bible/BibleReaderScreen.tsx contains `colors.bibleBackground`
    - src/components/audio/MiniPlayer.tsx does NOT contain hardcoded hex colors
    - src/components/audio/PlaybackControls.tsx does NOT contain hardcoded hex colors
    - src/components/audio/AudioPlayerBar.tsx uses `colors.*` tokens for all color values
    - src/components/bible/CompanionCard.tsx does NOT contain `shadows.card`
    - src/components/skeleton/Skeleton.tsx uses `colors.cardBackground` or theme tokens for shimmer
    - No file in this task contains `borderRadius: 12` or `borderRadius: 16` or `borderRadius: 24` or `borderRadius: 32` (old values)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    BibleReader glass effects use theme tokens. All audio, bible, and skeleton components use warm palette, reduced radius, no decorative shadows, no hardcoded colors. TypeScript and lint pass.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- grep for hardcoded `#ffffff`, `#fff`, `rgba(255` across all modified files returns zero matches
- grep for `shadows.card` spread across all modified files returns zero matches
- grep for old radius values (12, 16, 24, 32 as borderRadius) across all modified files returns zero matches
</verification>

<success_criteria>
1. HomeScreen verse display uses Lora serif via typography.readingDisplay
2. BibleReaderScreen has zero hardcoded rgba values — all replaced with theme tokens
3. All audio components use warm palette tokens
4. Tab bar uses warm theme colors
5. No file in this plan contains hardcoded hex colors, old radius values, or shadows.card spreads
6. npm run release:verify passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-reverential-theme-typography/15-02-SUMMARY.md`
</output>
