---
phase: 15-reverential-theme-typography
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - src/screens/learn/CourseListScreen.tsx
  - src/screens/learn/CourseDetailScreen.tsx
  - src/screens/learn/FieldOverviewScreen.tsx
  - src/screens/learn/FourFieldsJourneyScreen.tsx
  - src/screens/learn/FourFieldsLessonViewScreen.tsx
  - src/screens/learn/GroupListScreen.tsx
  - src/screens/learn/GroupDetailScreen.tsx
  - src/screens/learn/GroupSessionScreen.tsx
  - src/screens/learn/LessonViewScreen.tsx
  - src/screens/more/MoreScreen.tsx
  - src/screens/more/AboutScreen.tsx
  - src/screens/more/ProfileScreen.tsx
  - src/screens/more/ReadingActivityScreen.tsx
  - src/screens/more/LocalePreferencesScreen.tsx
  - src/screens/more/PrivacyPreferencesScreen.tsx
  - src/screens/auth/SignInScreen.tsx
  - src/screens/auth/SignUpScreen.tsx
  - src/screens/onboarding/LocaleSetupFlow.tsx
  - src/components/fourfields/FieldCard.tsx
  - src/components/fourfields/JourneyPath.tsx
  - src/components/fourfields/PracticeCard.tsx
  - src/components/fourfields/TakeawayCard.tsx
  - src/components/fourfields/LessonSectionRenderer.tsx
  - src/components/privacy/PrivacyLockScreen.tsx
  - src/components/ErrorBoundary.tsx
autonomous: false
requirements:
  - M2-DESIGN-01

must_haves:
  truths:
    - "Every screen in the app uses theme tokens for all colors — no hardcoded hex values remain"
    - "Learn/Harvest screens use warm palette with reduced radius and no nested cards"
    - "More/Settings/Profile screens use warm palette and stripped visual patterns"
    - "Auth and onboarding screens use warm palette"
    - "Four Fields components have hardcoded icon colors (#FFD700, #4169E1, #9932CC) replaced with theme tokens"
    - "User can visually verify the complete reverential theme across all three modes on device"
  artifacts:
    - path: "src/screens/learn/GroupSessionScreen.tsx"
      provides: "Warm-palette group session with theme-aware icon colors"
      contains: "colors.warning"
    - path: "src/components/fourfields/FieldCard.tsx"
      provides: "Theme-aware field card with no hardcoded colors"
      contains: "colors.accentPrimary"
    - path: "src/screens/more/MoreScreen.tsx"
      provides: "Warm-palette More screen"
      contains: "colors.background"
  key_links:
    - from: "src/screens/learn/GroupSessionScreen.tsx"
      to: "src/contexts/ThemeContext.tsx"
      via: "Theme tokens replacing 9 hardcoded icon/background colors"
      pattern: "colors\\.(warning|accentPrimary|primaryText)"
    - from: "src/components/fourfields/FieldCard.tsx"
      to: "src/contexts/ThemeContext.tsx"
      via: "Theme tokens replacing 5 hardcoded colors"
      pattern: "colors\\."
---

<objective>
Complete the screen-by-screen visual sweep across all remaining surfaces: Learn/Harvest stack, More stack, auth screens, onboarding, Four Fields components, and utility components. Then verify the complete reverential theme visually on device.

Purpose: This plan ensures D-16 (all screens swept) is fully delivered. No screen in the app should retain the old cool-toned palette, excessive radius, or hardcoded colors after this plan.

Output: Every screen and component in the app uses the warm reverential palette via theme tokens. Visual checkpoint confirms the transformation across all three theme modes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-reverential-theme-typography/15-CONTEXT.md
@.planning/phases/15-reverential-theme-typography/15-RESEARCH.md
@.planning/phases/15-reverential-theme-typography/15-01-SUMMARY.md
@.planning/phases/15-reverential-theme-typography/15-02-SUMMARY.md

<interfaces>
<!-- Available from Plans 01 and 02 -->

ThemeColors includes: background, cardBackground, cardBorder, primaryText, secondaryText,
accentGreen, accentPrimary, accentSecondary, accentTertiary, tabActive, tabInactive,
error, success, warning, overlay, bibleBackground, bibleSurface, bibleElevatedSurface,
bibleDivider, biblePrimaryText, bibleSecondaryText, bibleAccent, bibleControlBackground,
glassBackground.

radius: { xs: 2, sm: 4, md: 8, lg: 12, pill: 999 }
shadows.card: {} (empty)
shadows.floating: { ... } (only for modals/overlays)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep Learn/Harvest screens and Four Fields components</name>
  <files>
    src/screens/learn/CourseListScreen.tsx,
    src/screens/learn/CourseDetailScreen.tsx,
    src/screens/learn/FieldOverviewScreen.tsx,
    src/screens/learn/FourFieldsJourneyScreen.tsx,
    src/screens/learn/FourFieldsLessonViewScreen.tsx,
    src/screens/learn/GroupListScreen.tsx,
    src/screens/learn/GroupDetailScreen.tsx,
    src/screens/learn/GroupSessionScreen.tsx,
    src/screens/learn/LessonViewScreen.tsx,
    src/components/fourfields/FieldCard.tsx,
    src/components/fourfields/JourneyPath.tsx,
    src/components/fourfields/PracticeCard.tsx,
    src/components/fourfields/TakeawayCard.tsx,
    src/components/fourfields/LessonSectionRenderer.tsx
  </files>
  <read_first>
    src/screens/learn/GroupSessionScreen.tsx,
    src/screens/learn/CourseListScreen.tsx,
    src/screens/learn/FourFieldsLessonViewScreen.tsx,
    src/screens/learn/FieldOverviewScreen.tsx,
    src/screens/learn/GroupDetailScreen.tsx,
    src/screens/learn/FourFieldsJourneyScreen.tsx,
    src/screens/learn/CourseDetailScreen.tsx,
    src/screens/learn/GroupListScreen.tsx,
    src/screens/learn/LessonViewScreen.tsx,
    src/components/fourfields/FieldCard.tsx,
    src/components/fourfields/JourneyPath.tsx,
    src/components/fourfields/PracticeCard.tsx,
    src/components/fourfields/TakeawayCard.tsx,
    src/components/fourfields/LessonSectionRenderer.tsx,
    src/contexts/ThemeContext.tsx,
    src/design/system.ts
  </read_first>
  <action>
**Per D-13, D-14, D-15, D-16, D-17, D-18 — apply the same reverential sweep pattern used in Plan 02:**

For every file:
1. Replace ALL hardcoded hex colors with appropriate `colors.*` tokens
2. Replace ALL old-scale `borderRadius` values with new tokens (xs:2, sm:4, md:8, lg:12)
3. Remove `...shadows.card` spreads
4. Flatten nested card-in-card patterns using spacing and dividers
5. Do NOT change layout, navigation, or feature logic

**GroupSessionScreen.tsx — highest priority (9 known hardcoded colors):**
Replace the semantic icon colors per research recommendation:
- `#FFD700` (gold/insight icon) → `colors.warning`
- `#4169E1` (blue/back action) → `colors.accentPrimary`
- `#9932CC` (purple/prayer) → `colors.accentPrimary` (no separate spiritual token — keep unified accent)
- `#fff` on colored buttons → keep as white ONLY if the button background is `colors.accentPrimary` (white on accent is always correct). Otherwise use `colors.primaryText`.
- Any remaining `#ffffff` → `colors.primaryText` or `colors.cardBackground` depending on context

**FourFieldsLessonViewScreen.tsx (2 known hardcoded: #FFD700, #fff):**
- `#FFD700` → `colors.warning`
- `#fff` → `colors.primaryText`
- Lesson body text that is reading content (not UI labels): ensure it uses `typography.readingBody` for serif rendering

**FieldOverviewScreen.tsx (1 known: #fff):**
- `#fff` → `colors.primaryText` or `colors.cardBackground`

**GroupDetailScreen.tsx (1 known: #fff):**
- `#fff` → `colors.primaryText`

**CourseListScreen.tsx (1 known: #fff):**
- `#fff` → `colors.primaryText`

**FourFieldsJourneyScreen.tsx (2 known: #fff):**
- `#fff` → `colors.primaryText`

**FieldCard.tsx component (5 known hardcoded colors):**
- Replace all 5 hardcoded values with appropriate `colors.*` tokens
- The field-specific background tints: if the component uses different background colors per field (Entry/Gospel/Discipleship/Kingdom), map them to a minimal set: `colors.cardBackground` for the card surface, `colors.accentPrimary` for the active/highlighted field, `colors.cardBorder` for inactive borders

**JourneyPath.tsx (2 known rgba values):**
- Replace rgba path/line colors with `colors.cardBorder` for the path stroke and `colors.accentPrimary` for progress

**PracticeCard.tsx (1 known: #fff):**
- `#fff` → `colors.primaryText`

**TakeawayCard.tsx, LessonSectionRenderer.tsx, CourseDetailScreen.tsx, GroupListScreen.tsx, LessonViewScreen.tsx:**
- Apply the same general sweep: replace hardcoded colors, reduce radius, remove shadows, flatten nested cards
- Any lesson body text that renders scripture or reading content: use `typography.readingBody` (Lora serif)
  </action>
  <verify>
    <automated>cd /Users/dev/conductor/workspaces/EveryBible/columbus && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/learn/GroupSessionScreen.tsx does NOT contain `#FFD700` or `#4169E1` or `#9932CC`
    - src/screens/learn/GroupSessionScreen.tsx contains `colors.warning` (replacing gold)
    - src/screens/learn/FourFieldsLessonViewScreen.tsx does NOT contain `#FFD700` or `#fff`
    - src/components/fourfields/FieldCard.tsx does NOT contain any hardcoded hex color (no `#` followed by 3+ hex chars)
    - src/components/fourfields/JourneyPath.tsx does NOT contain `rgba(` that is not wrapped in a colors.* token
    - Every file in this task uses `colors.background` or `colors.cardBackground` for backgrounds (not hardcoded)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    All Learn/Harvest screens and Four Fields components use warm theme tokens. GroupSessionScreen's 9 hardcoded colors are replaced. FieldCard's 5 hardcoded colors are replaced. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep More stack, auth, onboarding, and utility components</name>
  <files>
    src/screens/more/MoreScreen.tsx,
    src/screens/more/AboutScreen.tsx,
    src/screens/more/ProfileScreen.tsx,
    src/screens/more/ReadingActivityScreen.tsx,
    src/screens/more/LocalePreferencesScreen.tsx,
    src/screens/more/PrivacyPreferencesScreen.tsx,
    src/screens/auth/SignInScreen.tsx,
    src/screens/auth/SignUpScreen.tsx,
    src/screens/onboarding/LocaleSetupFlow.tsx,
    src/components/privacy/PrivacyLockScreen.tsx,
    src/components/ErrorBoundary.tsx
  </files>
  <read_first>
    src/screens/more/MoreScreen.tsx,
    src/screens/more/AboutScreen.tsx,
    src/screens/more/ProfileScreen.tsx,
    src/screens/more/ReadingActivityScreen.tsx,
    src/screens/more/LocalePreferencesScreen.tsx,
    src/screens/more/PrivacyPreferencesScreen.tsx,
    src/screens/auth/SignInScreen.tsx,
    src/screens/auth/SignUpScreen.tsx,
    src/screens/onboarding/LocaleSetupFlow.tsx,
    src/components/privacy/PrivacyLockScreen.tsx,
    src/components/ErrorBoundary.tsx,
    src/contexts/ThemeContext.tsx,
    src/design/system.ts
  </read_first>
  <action>
**Same sweep pattern as all prior tasks. For each file:**
1. Replace ALL hardcoded hex colors with `colors.*` tokens
2. Replace ALL old-scale borderRadius with new tokens
3. Remove shadows.card spreads
4. Flatten nested cards
5. Do NOT change layout, navigation, or feature logic

**MoreScreen.tsx (1 known: #ffffff):**
- `#ffffff` → `colors.primaryText` or `colors.cardBackground`
- Settings row items: reduce border radius, use `colors.cardBorder` for dividers between rows instead of card wrapping

**ReadingActivityScreen.tsx (2 known: #ffffff):**
- Replace hardcoded whites with appropriate theme tokens
- Calendar/streak elements: use `colors.accentPrimary` for active days, `colors.cardBackground` for inactive
- Streak number display: consider using `typography.readingDisplay` (Lora italic) for the streak count hero number to bring serif feel

**ProfileScreen.tsx:**
- Replace any hardcoded colors
- Profile section: reduce card radius, use `colors.cardBackground` with `colors.cardBorder` border

**AboutScreen.tsx:**
- Replace any hardcoded colors
- Reduce radius on any card elements

**LocalePreferencesScreen.tsx, PrivacyPreferencesScreen.tsx:**
- Replace any hardcoded colors
- Settings-style rows: consistent with SettingsScreen treatment from Plan 01

**Auth screens (SignInScreen.tsx, SignUpScreen.tsx):**
- Replace any hardcoded colors
- Input fields: `colors.cardBackground` background, `colors.cardBorder` border, `radius.sm` (4) border radius
- Buttons: `colors.accentPrimary` background for primary action, `radius.sm` border radius
- Background: `colors.background`
- Text: `colors.primaryText` and `colors.secondaryText`

**LocaleSetupFlow.tsx (onboarding):**
- Replace any hardcoded colors
- Language/country selection items: `colors.cardBackground` with `colors.accentPrimary` highlight for selected
- Reduce border radius

**PrivacyLockScreen.tsx:**
- Replace any hardcoded colors
- PIN entry: `colors.cardBackground` for digit boxes, `colors.accentPrimary` for filled dots
- Background: `colors.background`

**ErrorBoundary.tsx:**
- Replace any hardcoded colors with theme tokens (may need to handle the case where ThemeProvider is not available — use `darkColors` import as fallback)

**Final sweep validation after all files are updated:**
Run a project-wide search for remaining hardcoded colors to catch any missed instances. Search patterns:
- `#ffffff` (case-insensitive)
- `#fff` as a standalone color value
- `#FFD700`, `#4169E1`, `#9932CC`
- `rgba(255` (white-based rgba)
Fix any remaining instances found.
  </action>
  <verify>
    <automated>cd /Users/dev/conductor/workspaces/EveryBible/columbus && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/more/MoreScreen.tsx does NOT contain `#ffffff`
    - src/screens/more/ReadingActivityScreen.tsx does NOT contain `#ffffff`
    - src/screens/auth/SignInScreen.tsx uses `colors.background` and `colors.accentPrimary`
    - src/screens/auth/SignUpScreen.tsx uses `colors.background` and `colors.accentPrimary`
    - src/screens/onboarding/LocaleSetupFlow.tsx uses `colors.background` and `colors.cardBackground`
    - src/components/privacy/PrivacyLockScreen.tsx uses `colors.background`
    - src/components/ErrorBoundary.tsx uses theme colors (either via useTheme or darkColors import)
    - No .tsx file under src/screens/ or src/components/ contains `borderRadius: 12` or `borderRadius: 16` or `borderRadius: 24` or `borderRadius: 32`
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    All remaining screens and components use warm theme tokens. No hardcoded hex colors remain across the entire codebase. No old-scale radius values remain. TypeScript and lint pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete reverential theme</name>
  <action>User verifies the complete reverential theme transformation across all screens and all three theme modes on device or simulator.</action>
  <what-built>
    Complete reverential theme transformation across all screens and components:
    - Three warm palettes (Dark as hero, Light, Low-light/Sepia)
    - Bundled Lora serif font for all reading surfaces
    - Print-editorial radius (2-8px instead of 12-32px)
    - Stripped decorative shadows, nested cards, and hardcoded colors
    - Three-way theme selector in Settings
  </what-built>
  <how-to-verify>
    1. Run `npx expo start` and open on iOS simulator (or device)
    2. Navigate to Settings > Theme and verify three options appear: Dark, Light, Low-light
    3. **Dark mode (default):** Verify warm near-black backgrounds (not cool blue-grey), cream text (not pure white), warm red accent on interactive elements only
    4. **Light mode:** Verify warm cream/off-white backgrounds (not clinical #f5f7fa), warm brown-grey text
    5. **Low-light mode:** Verify amber/sepia tones — should feel like reading by candlelight
    6. **Bible reader:** Open any chapter — verify Lora serif font for body text, verse numbers, section headings. Verify glass header effects are warm-tinted, not cool
    7. **Home screen:** Verify verse-of-the-day uses Lora Italic serif. No nested card-in-card patterns
    8. **Learn/Harvest:** Open Four Fields journey, verify warm palette, no gold/blue/purple hardcoded icon colors
    9. **More/Profile:** Verify consistent warm palette across all settings screens
    10. **Switch between all 3 themes** on each screen to verify no hardcoded colors bleed through
    11. Close and reopen the app — verify theme preference persists
    12. Compare against reference screenshots in `.context/attachments/` — the app should feel closer to Glorify/ESV warmth than the previous cool tech-app aesthetic
  </how-to-verify>
  <verify>Visual inspection on device across all three theme modes</verify>
  <done>User approves the reverential theme across all screens and all three modes</done>
  <resume-signal>Type "approved" if the reverential theme looks correct across all three modes, or describe specific issues (e.g., "Low-light text too dark on GroupSession screen")</resume-signal>
</task>

</tasks>

<verification>
- `npm run release:verify` passes (lint + typecheck + release regression suite)
- Project-wide grep for `#ffffff`, `#FFD700`, `#4169E1`, `#9932CC` in .tsx files returns zero matches
- Project-wide grep for `borderRadius: 12` or `borderRadius: 16` or `borderRadius: 24` or `borderRadius: 32` in .tsx files returns zero matches
- Visual verification across all three theme modes on device/simulator
</verification>

<success_criteria>
1. Every screen listed in D-16 has been swept with warm palette, reduced radius, and stripped patterns
2. GroupSessionScreen's 9 hardcoded colors and FieldCard's 5 hardcoded colors are all replaced with theme tokens
3. No .tsx file under src/ contains hardcoded hex color values outside of ThemeContext.tsx palette definitions
4. All three theme modes (Dark, Light, Low-light) render correctly across every screen
5. Theme preference persists across app restart
6. npm run release:verify passes
7. User approves visual quality on device
</success_criteria>

<output>
After completion, create `.planning/phases/15-reverential-theme-typography/15-03-SUMMARY.md`
</output>
