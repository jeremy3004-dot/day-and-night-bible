---
phase: 26
plan: 01
type: fix
autonomous: true
---

# Phase 26 Plan 01: UX Completeness - Navigation Gaps

## Objective

Fix two disconnected UI stubs that degrade user experience:
1. AnnotationsScreen annotation cards are tappable but do nothing
2. LessonDetailScreen "Add Note" button shows a coming-soon alert

## Tasks

### Task 1: AnnotationsScreen tap navigation
- Import `rootNavigationRef` from `../../navigation/rootNavigation`
- Add `navigateToBible` helper that calls `rootNavigationRef.navigate('Bible', { screen: 'BibleReader', params: { bookId, chapter, focusVerse } })`
- Wire `onPress` on the annotation card `TouchableOpacity`

### Task 2: LessonDetailScreen note button removal
- Remove `Alert` from RN imports (only used for the stub)
- Remove `onAddNote` prop from `ApplicationSectionProps` interface
- Remove `onAddNote` parameter from `ApplicationSection` function signature
- Remove the `idx === 3` branch that rendered the "Add Note" button
- Remove `onAddNote` from the call site in the screen body

## Verification

- `npm run typecheck` — no errors
- `npm run lint` — no new errors (pre-existing Deno errors unaffected)
