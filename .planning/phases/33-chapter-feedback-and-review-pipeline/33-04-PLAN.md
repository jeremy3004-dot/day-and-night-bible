---
phase: 33-chapter-feedback-and-review-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - app.json
  - ios/EveryBible.xcodeproj/project.pbxproj
  - .planning/STATE.md
autonomous: false
requirements: [FDBK-01, FDBK-02, FDBK-03, FDBK-04]
user_setup:
  - "Be signed into the correct App Store Connect team if local submit needs refreshed credentials."
  - "Be signed into GitHub CLI if branch push or PR creation requires refreshed auth."
must_haves:
  truths:
    - "The chapter feedback feature ships in a new iOS TestFlight build whose build number is strictly higher than the latest uploaded build."
    - "Release verification passes before the TestFlight upload is attempted."
    - "The resulting build is confirmed in App Store Connect with the intended TestFlight distribution path before it is described as available."
    - "GitHub is updated with the implementation branch and a PR or equivalent branch update containing the feature."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "release evidence, build number, TestFlight status, and GitHub update note"
  key_links:
    - from: "app.json"
      to: "ios/EveryBible.xcodeproj/project.pbxproj"
      via: "aligned iOS build number and release metadata"
      pattern: "buildNumber"
---

<objective>
Ship the completed chapter feedback feature through the repo's real release path, not just local verification.

Purpose: The user explicitly wants this feature pushed to TestFlight and reflected on GitHub after implementation. This plan turns that into an explicit deliverable with the repo's iOS safety rules intact.

Output: green release verification, incremented iOS build metadata, local TestFlight submission with confirmed distribution state, and a pushed GitHub branch/PR update.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-chapter-feedback-and-review-pipeline/33-01-PLAN.md
@.planning/phases/33-chapter-feedback-and-review-pipeline/33-02-PLAN.md
@.planning/phases/33-chapter-feedback-and-review-pipeline/33-03-PLAN.md

@app.json
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full release gate and prepare a valid iOS build number</name>
  <files>app.json, ios/EveryBible.xcodeproj/project.pbxproj, .planning/STATE.md</files>
  <read_first>CLAUDE.md, package.json, app.json, ios/EveryBible.xcodeproj/project.pbxproj</read_first>
  <action>
    Before any build:
    - run `npm run release:verify`
    - inspect the latest uploaded iOS build number
    - raise `expo.ios.buildNumber` so it is strictly higher
    - keep iOS metadata aligned where the repo expects it

    Then produce a local-only iOS build and submit it to TestFlight using the repo's local-build rule. Never use cloud EAS build paths.

    After submission, confirm in App Store Connect:
    - build ID
    - build number
    - `processingState`
    - assigned TestFlight group or direct tester attachment

    Record the evidence in the eventual summary and in `.planning/STATE.md`.
  </action>
  <verify>
    <automated>npm run release:verify</automated>
  </verify>
  <acceptance_criteria>
    - The release gate passes before submission.
    - The uploaded build number is higher than the last uploaded build number.
    - TestFlight distribution is verified in App Store Connect, not inferred from submit logs.
  </acceptance_criteria>
  <done>The feature exists in a real TestFlight build with evidence, not just in local commits.</done>
</task>

<task type="auto">
  <name>Task 2: Push the branch and update GitHub with the finished feature</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md</read_first>
  <action>
    After release verification and TestFlight submission are complete:
    - commit the implementation cleanly
    - push the branch to GitHub
    - open or update the pull request so the feature is reviewable remotely
    - include in the PR summary:
      - feature behavior
      - spreadsheet/export assumptions
      - release verification results
      - TestFlight build number and distribution evidence

    Update `.planning/STATE.md` with the branch/PR status if that becomes the active continuation point.
  </action>
  <verify>
    <automated>git status --short && git branch --show-current</automated>
  </verify>
  <acceptance_criteria>
    - GitHub has the pushed branch.
    - A PR or explicit branch update exists with the release/TestFlight evidence.
  </acceptance_criteria>
  <done>The feature is not trapped locally; GitHub reflects the finished work and release evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run release:verify` passes.
- [ ] Latest uploaded build number was inspected and incremented safely.
- [ ] TestFlight build visibility is confirmed in App Store Connect.
- [ ] Branch is pushed and GitHub is updated with the feature summary.
</verification>

<success_criteria>
- The feature is implemented, verified, distributed to TestFlight, and pushed to GitHub.
- Release evidence includes concrete build IDs and visibility state.
</success_criteria>

<output>
After completion, create `.planning/phases/33-chapter-feedback-and-review-pipeline/33-04-SUMMARY.md`
</output>
